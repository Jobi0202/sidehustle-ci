name: _selftest
on:
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write
jobs:
  call:
    uses: ./.github/workflows/_probe.yml
    secrets: inherit
