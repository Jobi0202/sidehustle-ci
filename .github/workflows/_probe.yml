name: _probe
on:
  workflow_call:
    secrets:
      DEEPSEEK_API_KEY:
        required: false
jobs:
  ping:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - run: echo ok
