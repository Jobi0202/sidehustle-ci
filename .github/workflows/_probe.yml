name: _probe
on:
  workflow_call:
    secrets:
      DEEPSEEK_API_KEY:
        required: false
jobs:
  ping:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - run: echo ok
